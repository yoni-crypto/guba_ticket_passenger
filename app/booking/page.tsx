export default function BookingPage() {
  return (
    <section className="mx-auto flex min-h-[60vh] max-w-4xl flex-col justify-center gap-4 px-6 py-16 text-foreground">
      <h1 className="text-3xl font-semibold md:text-4xl">Booking</h1>
      <p className="text-base text-foreground/70 md:text-lg">
        This route is ready for implementing the booking journey. Add server actions, data
        fetching logic, and client components as new features are scoped.
      </p>
    </section>
  );
}
